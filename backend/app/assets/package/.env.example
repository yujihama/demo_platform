# Demo Platform Workflow Runtime Environment
# Copy this file to `.env` before running `docker compose up`.

# -------------------------------------------------------------------
# Container image overrides
# Uncomment to pin to a specific image tag when running locally.
# ENGINE_IMAGE=ghcr.io/demo-platform/workflow-engine:latest
# FRONTEND_IMAGE=ghcr.io/demo-platform/workflow-frontend:latest

# -------------------------------------------------------------------
# Port configuration
ENGINE_API_PORT=8000
FRONTEND_PORT=3000

# -------------------------------------------------------------------
# LLM providers
# Toggle mock vs. live providers when the runtime supports them.
WORKFLOW_PROVIDER=mock
OPENAI_API_KEY=
AZURE_OPENAI_API_KEY=

# -------------------------------------------------------------------
# Dify environment (mock by default)
DIFY_BASE_URL=http://engine:8000/mock/dify
DIFY_API_KEY=
