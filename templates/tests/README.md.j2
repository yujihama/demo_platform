# Playwright テスト実行方法

1. 依存関係をインストールします。

   ```bash
   npm install
   npx playwright install
   ```

2. テストを実行します。

   ```bash
   npx playwright test
   ```

Python から Faker ベースのテストデータを生成したい場合は `pip install -r requirements.txt` を実行し、`python -m data.generator` でサンプルデータを確認できます。

