export type InvoiceField = {
  name: string;
  label: string;
  type: string;
  required?: boolean;
  options?: string[];
};

export type WizardStep = {
  id: string;
  label: string;
  description: string;
};

export type ValidationRule = {
  id: string;
  description: string;
  severity: string;
};

