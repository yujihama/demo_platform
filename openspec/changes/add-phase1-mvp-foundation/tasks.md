## 1. Implementation
- [x] 1.1 プラットフォーム環境セットアップ
  - [x] 1.1.1 プラットフォーム開発用の `docker-compose.yml` を作成し、フロント/バックエンドコンテナのホットリロード設定を含める
  - [x] 1.1.2 `.env.example` を整備し、必要な環境変数とシークレットのプレースホルダーを用意する
  - [x] 1.1.3 `README.md` にローカル環境構築手順と起動方法を追記する
- [x] 1.2 設定基盤
  - [x] 1.2.1 `config/` ディレクトリを初期化し、`features.yaml`・`llm.yaml`・`dify.yaml` の雛形を追加する
  - [x] 1.2.2 ConfigManager（または同等クラス）を実装し、YAML 設定を読み込んで Pydantic モデルへマッピングする
  - [x] 1.2.3 Phase 1 デフォルトとしてモックエージェント/モック Dify を有効化する初期値を設定する
- [x] 1.3 バックエンド生成フロー
  - [x] 1.3.1 `POST /api/generate`・`GET /api/generate/{job_id}` の FastAPI ルーターと OpenAPI スキーマを定義する
  - [x] 1.3.2 モックエージェント層を実装し、固定仕様 JSON を読み込んで仕様書モデルへ変換する
  - [x] 1.3.3 テンプレートレンダリングサービス（Jinja2）を実装し、生成物ディレクトリを構築する
  - [x] 1.3.4 パッケージングエンジンを実装し、Zip 化と `metadata.json` 出力、進捗更新を行う
  - [x] 1.3.5 進捗ポーリング用ジョブ状態を Phase 1 では Python のインメモリ辞書で管理し、`GET` 応答に含める
  - [x] 1.3.6 UI モックプレビュー用の固定画像/HTML を返すサービスを実装し、ステップ3向けに公開する
- [x] 1.4 テンプレート整備
  - [x] 1.4.1 React フロントエンドテンプレートを作成し、UI パーツ・状態管理の埋め込みポイントを定義する
  - [x] 1.4.2 FastAPI バックエンドテンプレートを用意し、Phase 3 で呼び出す Dify API のプレースホルダー（モック関数）を配置する
  - [x] 1.4.3 Playwright テストテンプレートと Faker ベースのテストデータジェネレーターを準備する
  - [x] 1.4.4 Dockerfile・docker-compose・ログ設定ファイルのテンプレートを追加する
- [x] 1.5 フロントエンドウィザード
  - [x] 1.5.1 React 18 + MUI v5 を前提にステッパー/ルーティングの骨組みを構築する
  - [x] 1.5.2 ステップ1要件入力フォームと API 呼び出し・バリデーションを実装する
  - [x] 1.5.3 ステップ2（アプリ設計進捗表示）画面を実装し、バックエンド進行状況を可視化する
  - [x] 1.5.4 ステップ3モックプレビュー画面と承認/差戻しの遷移制御を実装する
  - [x] 1.5.5 進捗ステップ（4〜6）のポーリングとログ表示 UI を実装する
  - [x] 1.5.6 ステップ7ダウンロード画面と再実行導線を実装し、Zip URL を接続する
  - [x] 1.5.7 バックエンドエラーをユーザーフレンドリーなメッセージへ変換し、対処法を提示する UI を実装する
- [x] 1.6 CLI フロー
  - [x] 1.6.1 `backend/cli.py` に Click などで `generate` コマンドを定義する
  - [x] 1.6.2 設定ファイル読込と必須項目検証、既定値の適用ロジックを実装する
  - [x] 1.6.3 バックエンド生成パイプラインを再利用し、進捗ログ/成功メッセージ/エラー出力を整備する
  - [x] 1.6.4 生成物保存先の存在確認と `output/{user_id}/{project_id}` の自動生成を実装する
- [x] 1.7 E2E テストと開発支援
  - [x] 1.7.1 Playwright + pytest のセットアップ、`tests/e2e` ディレクトリ構成、共通フィクスチャを作成する
  - [x] 1.7.2 UI ウィザードのハッピーパステストと Zip ダウンロード検証を実装する
  - [x] 1.7.3 CLI `generate` のハッピーパス検証と成果物内容チェックを実装する
  - [x] 1.7.4 テストデータ生成ユーティリティとクリーンアップ処理を追加する
  - [x] 1.7.5 UI/CLI テストを一括実行する PowerShell スクリプトまたは Makefile ターゲットを用意する
  - [x] 1.7.6 生成されたアプリの `docker-compose up` を自動実行し、主要エンドポイントの疎通を確認するテストを追加する
- [x] 1.8 推奨改善（任意）
  - [x] 1.8.1 要件入力画面にサンプルプロンプトを表示するコンポーネントを実装する
  - [x] 1.8.2 Phase 2 を見据えた LLM Factory のモック実装を追加し、将来の差し替えポイントを固定する
  - [x] 1.8.3 プラットフォーム全体のロギング設定（pino/Loguru）を整備し、統一ログフォーマットを出力する

## 2. Validation
- [x] 2.1 OpenSpec 仕様差分の検証（`openspec validate add-phase1-mvp-foundation --strict`）
- [ ] 2.2 UI 経由の請求書検証アプリ生成が成功し、Zip をダウンロードできることを確認する
- [ ] 2.3 CLI `generate` コマンドで同一成果物が生成され、Docker 起動が成功することを確認する
- [ ] 2.4 自動テストで生成アプリの `docker-compose up` と基本疎通が通ることを確認する

