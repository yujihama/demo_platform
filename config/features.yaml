phase: mvp

agents:
  use_mock: true
  allow_llm_toggle: true
  mock_spec_path: ./mock/specs/invoice-verification.json

generation:
  output_root: ./output
  package_name_template: "invoice-verification"
  enable_playwright: true

frontend:
  base_url: http://localhost:5173
  polling_interval_seconds: 2

backend:
  base_url: http://localhost:8000
  log_level: INFO

